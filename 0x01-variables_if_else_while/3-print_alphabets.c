#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char l;

	for (l = 'a'; c <= 'z'; c++)
		putchar(l);
	for (l = 'A'; c <= 'Z'; c++)
		putchar(l);
	putchar('\n');
	return (0);

}
